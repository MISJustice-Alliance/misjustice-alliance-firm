models:
  primary:
    id: gpt-4o-mini
    provider: openai
    purpose: Default reasoning, drafting, and sequencing

  config:
    temperature: 0.4
    max_tokens: 2048
    top_p: 1.0
    frequency_penalty: 0.0
    presence_penalty: 0.0

  data_tier: T3

  fallback:
    id: gpt-4o-mini
    provider: openai
    note: Same model; reduce max_tokens to 1024 under load
